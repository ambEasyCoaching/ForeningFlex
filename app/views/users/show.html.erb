<h3>Medlem » Mine medlemsinformationer</h3>

<%= form_for @user, :url => account_path, :html => { :multipart => true } do |f| %>
  <% unless f.error_messages.empty? %>
    <div class="alert alert-danger">
      <%= f.error_messages %>
    </div>
  <% end %>
  <%= render :partial => "edit", :object => f %>
  <br>
  <%= f.submit "Gem mine medlemsinformationer", :class => "btn btn-success" %>
<% end %>
